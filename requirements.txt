PySide6>=6.6
# Audio-Player: Qt Multimedia (MP3/WAV) — bei Import-Fehler:
# pip install PySide6-Addons
PySide6-Addons>=6.6
pyserial>=3.5
# MP3-Encoder fuer den Audio-Recorder: wir nehmen erst WAV auf (uncompressed
# PCM), normalisieren das Signal in Python (Soft-Compressor + Limiter) und
# encoden dann nach MP3. So vermeiden wir Pegelverluste durch das schwache
# QMediaRecorder-MP3-Encoding und brauchen kein externes ffmpeg.
lameenc>=1.7
# Windows: System-Lautstärke/Stumm pro Soundgerät (Soundeinstellungen)
pycaw>=20240210; sys_platform == "win32"
comtypes>=1.4; sys_platform == "win32"
